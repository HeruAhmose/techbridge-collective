name: Temporary NanoID 6.0.1 migration validation

on:
  push:
    branches:
      - deps/nanoid-6.0.1-20260817

permissions:
  contents: write

jobs:
  migrate-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: deps/nanoid-6.0.1-20260817
          fetch-depth: 0

      - name: Set up Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24'

      - name: Install pinned pnpm
        run: npm install --global pnpm@10.34.5

      - name: Assert live registry target
        shell: bash
        run: |
          set -euo pipefail
          latest="$(npm view nanoid version)"
          test "$latest" = "6.0.1" || {
            echo "Expected live nanoid latest=6.0.1; got $latest" >&2
            exit 1
          }
          echo "nanoid latest=$latest VERIFIED"

      - name: Upgrade NanoID
        run: pnpm add 'nanoid@6.0.1'

      - name: Verify frozen install
        run: pnpm install --frozen-lockfile

      - name: Verify exact migration version
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('node:fs');
          const file = 'node_modules/nanoid/package.json';
          const got = JSON.parse(fs.readFileSync(file, 'utf8')).version;
          const want = '6.0.1';
          if (got !== want) throw new Error(`${file}: expected ${want}; got ${got}`);
          console.log(`${file}: ${got} VERIFIED`);
          NODE

      - name: Format migration files
        run: pnpm exec prettier --write package.json pnpm-lock.yaml .github/workflows/tmp-nanoid-6.0.1.yml

      - name: Run repository CI
        run: pnpm run ci

      - name: Commit migration and remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          rm -- .github/workflows/tmp-nanoid-6.0.1.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json pnpm-lock.yaml .github/workflows/tmp-nanoid-6.0.1.yml
          git commit -m "chore(deps): migrate NanoID to 6.0.1"
          git push origin HEAD:deps/nanoid-6.0.1-20260817
