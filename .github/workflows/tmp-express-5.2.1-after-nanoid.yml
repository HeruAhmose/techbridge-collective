name: Temporary Express 5.2.1 post-NanoID validation

on:
  push:
    branches:
      - deps/express-5.2.1-after-nanoid-20260817

permissions:
  contents: write

jobs:
  migrate-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: deps/express-5.2.1-after-nanoid-20260817
          fetch-depth: 0

      - name: Set up Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24'

      - name: Install pinned pnpm
        run: npm install --global pnpm@10.34.5

      - name: Assert live registry targets
        shell: bash
        run: |
          set -euo pipefail
          test "$(npm view express version)" = "5.2.1"
          test "$(npm view @types/express version)" = "5.0.6"

      - name: Upgrade Express and types
        run: pnpm add 'express@^5.2.1' && pnpm add --save-dev '@types/express@5.0.6'

      - name: Verify frozen install
        run: pnpm install --frozen-lockfile

      - name: Verify exact migration versions and NanoID preservation
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('node:fs');
          const expected = new Map([
            ['node_modules/express/package.json', '5.2.1'],
            ['node_modules/@types/express/package.json', '5.0.6'],
            ['node_modules/nanoid/package.json', '6.0.1'],
          ]);
          for (const [file, want] of expected) {
            const got = JSON.parse(fs.readFileSync(file, 'utf8')).version;
            if (got !== want) throw new Error(`${file}: expected ${want}; got ${got}`);
            console.log(`${file}: ${got} VERIFIED`);
          }
          NODE

      - name: Format migration files
        run: pnpm exec prettier --write package.json pnpm-lock.yaml server/index.ts .github/workflows/tmp-express-5.2.1-after-nanoid.yml

      - name: Run repository CI
        run: pnpm run ci

      - name: Commit migration and remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          rm -- .github/workflows/tmp-express-5.2.1-after-nanoid.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json pnpm-lock.yaml server/index.ts .github/workflows/tmp-express-5.2.1-after-nanoid.yml
          git commit -m "chore(deps): migrate Express to 5.2.1 after NanoID 6"
          git push origin HEAD:deps/express-5.2.1-after-nanoid-20260817
